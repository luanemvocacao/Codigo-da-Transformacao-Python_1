# Modulo_14/produtos/models.py
from django.db import models # type: ignore

class Produto(models.Model):
    nome = models.CharField(max_length=150, verbose_name="Nome do Produto")
    descricao = models.TextField(blank=True, null=True, verbose_name="Descrição")
    preco = models.DecimalField(max_digits=10, decimal_places=2, verbose_name="Preço")
    quantidade = models.IntegerField(default=0, verbose_name="Quantidade em Stock")

    def __str__(self):
        return f"{self.nome} ({self.quantidade} un)"